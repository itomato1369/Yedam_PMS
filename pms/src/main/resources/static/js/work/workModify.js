/**
 *  
 */
document.addEventListener("DOMContentLoaded", function () {
  // 수정할 소요시간의 
  
  $(".datepicker").datepicker({
    format: "yyyy-mm-dd",
    language: "ko",
    autoclose: true,
  });
});
