<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pms.home.memo.mapper.HomeMemoMapper">
	<select id="findMemos" resultType="HomeMemoDto">
		SELECT
		memo_no,
		content,
		add_date,
		user_id
		FROM memo
		WHERE user_id = #{userId}
		ORDER BY add_date DESC, memo_no DESC
	</select>

</mapper>