<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pms.home.project.mapper.HomeProjectMapper">
	<select id="findProjects" resultType="HomeProjectDto">
		SELECT
		project_no,
		project_name,
		project_desc,
		project_code
		FROM project
		ORDER BY create_at DESC NULLS LAST, project_no DESC
	</select>

</mapper>